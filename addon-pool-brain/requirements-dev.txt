# Dev-only requirements (tests).
# The add-on itself pins its runtime deps in the Dockerfile.
pytest>=8.0
pytest-asyncio>=0.23
# Needed by tests/test_charts.py. The runtime image installs these via
# apk (py3-numpy + py3-matplotlib) for smaller layer size.
matplotlib>=3.8
numpy>=1.26
